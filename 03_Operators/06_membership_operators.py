# Membership operators are used to test if a value is present in a sequence (e.g., list, string).

# Operator	Description	Example
# in	True if value is present	x in y
# not in	True if value is not present	x not in y

# if(obj in name) {}

x = "Hello"
y = [1, 2, 3, 4, 5]

print("H" in x)
print("6 not in y:", 6 not in y)